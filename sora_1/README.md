# sora_bot

Sora Suite объединяет автоматизацию генерации контента, массовую загрузку роликов и вспомогательные утилиты в одном настольном приложении на PyQt6. Интерфейс делится на понятные разделы, каждая страница отвечает за конкретный этап пайплайна: от подготовки промптов и работы с несколькими профилями Chrome до автоскачивания и уведомлений в Telegram.

## Установка зависимостей

```bash
python -m venv .venv
source .venv/bin/activate  # или .venv\Scripts\activate в Windows
python -m pip install --upgrade pip
python -m pip install -r sora_suite/requirements.txt
```

> **Важно.** Для генерации изображений используется `google-genai==1.45.0`. Библиотека поддерживается на Python 3.9+ и корректно устанавливается на Windows, macOS и Linux.

## Быстрый запуск

1. Активируйте виртуальное окружение.
2. Запустите приложение командой `python sora_suite/app.py`.
3. Настройки и вспомогательные файлы появятся рядом с `sora_suite/` — конфиг (`app_config.yaml`), журналы воркеров и каталоги для выгрузок.

## Обзор интерфейса

### Верхняя панель
- Шесть лаконичных ярлыков (PRJ, RAW, BLR, MRG, IMG, RST) для мгновенного открытия ключевых каталогов проекта.
- Командная палитра и переключатель правой панели с быстрыми командами остаются всегда под рукой.
- Унифицированные кнопки «⚡ Старт выбранного» и «⛔ Стоп все» запускают или останавливают любой выбранный пайплайн.

### Навигация по разделам
- **🏠 Главная.** Краткое описание возможностей, карточки статистики и быстрые ссылки на рабочие пространства, пайплайн и журналы.
- **🗂️ Рабочие пространства.** Менеджер сессий Chrome: профили, порты CDP, собственные логи и быстрые действия доступны прямо на странице.
- **🧠 Пайплайн.** Все этапы (промпты, генерация, скачка, очистка, склейка, загрузка) настраиваются на отдельной странице с лимитами и выбором профиля.
- **🤖 Автоматизатор.** Конструктор последовательностей шагов по сессиям с сохранением сценариев.
- **🗒️ Логи сессий.** Просмотр последних строк из `submitted.log`, `failed.log` и `download.log` для каждого рабочего пространства.
- **📜 Журнал процессов.** Глобальный поток событий с текущим статусом и возможностью очистки/экспорта.
- **📝 Контент.** Редакторы `prompts.txt`, `image_prompts.txt`, `titles.txt` и история отправленных строк по каждому профилю.
- **📨 Telegram.** Конфигурация бота, отправка шаблонов, планирование сообщений и просмотр последних уведомлений.
- **⚙️ Настройки.** Группы вкладок для путей, генерации картинок, FFmpeg, автопостинга, Chrome, Telegram и интерфейса.

Заголовки групп в боковой навигации можно сворачивать: клик по строке изменяет иконку (▶/▼) и скрывает все подпункты категории — полезно, когда хочется оставить только часто используемые разделы.

Основной контент расположен между тремя областями сплиттера. Навигация слева, центральная страница и правая панель разделены ручками — можно растягивать зоны под свои задачи. Каждая страница отображается на отдельной «карточке» с мягким фоном, поэтому содержимое визуально отделено от фонового холста.

Правая панель (по желанию скрывается) хранит быстрые действия разделов, а командная палитра (`⌘/Ctrl + K`) позволяет моментально переключаться между страницами или запускать скрипты.

## Рабочий поток: сессии + пайплайн

Каждая сессия связывает свой профиль Chrome, порт DevTools, файлы промптов и журналы. На странице **«🗂️ Рабочие пространства»** доступны:
- мгновенный запуск отдельного окна Chrome;
- запуск воркеров (промпты, картинки, скачка, водяной знак) и просмотр последних событий;
- заметки и напоминания по конкретному рабочему месту.

В правой панели контекста отображается активная сессия: профиль Chrome, набор промптов, RAW-папка и состояние последнего задания. Быстрые кнопки позволяют открыть отдельное окно, отправить промпты, запустить скачивание или очистку прямо оттуда.

Отдельная страница **«🧠 Пайплайн»** содержит галочки этапов, лимиты скачки и склейки, быстрый запуск генерации/замены водяного знака и прогресс-бар. Заголовок показывает выбранный профиль Chrome, а в контекстной панели автоматически перечисляются активные этапы и текущие ограничения. Параллельная работа достигается тем, что для каждого профиля хранится собственный порт CDP, журнал использованных промптов и очередь задач.

## Автоген и генерация изображений

### Настройка Google AI Studio
1. Получите API-ключ в [Google AI Studio](https://aistudio.google.com/).
2. Откройте **Настройки → Генерация картинок** и укажите ключ, модель, параметры размера и лимиты.
3. При необходимости включите шаблоны стилей, сиды и уведомления о приближении суточной квоты.
4. Сохранённые значения автоматически попадут в окружение воркера `workers/autogen/main.py`.

### Последовательность работы
1. На странице «Промпты картинок» заполните `image_prompts.txt` (поддерживаются строки и JSON с полями `prompt`, `prompts`, `count`, `video_prompt`).
2. Нажмите **«Генерация картинок (Google)»** — воркер запишет результаты в `generated_images/` и обновит `manifest.json`.
3. После проверки изображений используйте **«Сохранить и запустить автоген (видео)»**. Скрипт сопоставит `prompts.txt`, `image_prompts.txt` и `manifest.json`, прикрепит изображения и отправит заявки в Sora.
4. Все операции учитывают включённые лимиты, задержки и выбранные сессии.

## Автоcкачивание и обработка карточек

В пайплайне можно сразу указать, сколько карточек нужно скачать (например, «последние 30»). Приложение плавно прокручивает страницу, пока не найдёт нужное количество элементов, проверяет уже сохранённые карточки и предупреждает, если новых роликов меньше запрошенного. Это помогает стабильно выгружать свежие видео даже при параллельной работе нескольких окон.

## Автоматизатор процессов

На отдельной странице **🤖 Автоматизатор** собираются последовательности действий. Каждый шаг можно настроить индивидуально:

- выбрать, для каких сессий выполнить вставку промптов, генерацию картинок или комбинированный запуск автогена;
- указать, какие сессии должны запускать скачку видео (с собственным лимитом на шаг) или замену водяного знака;
- добавить глобальные этапы — блюр по пресету и склейку с нужным размером группы.

Шаги выполняются последовательно: если какой-то пункт завершился с ошибкой, цепочка останавливается и показывает, на чём именно произошла остановка. Автоматизатор поддерживает параллельные окна Chrome: для каждой сессии используется её порт CDP, собственные папки и настройки лимитов.

## Замена водяного знака без блюра

Отдельная вкладка **🧼 Водяной знак** отвечает за восстановление кадров вместо размытия. Алгоритм работает так:

1. Загружается шаблон водяного знака (PNG с альфа-каналом или любое изображение) и подбираются параметры сопоставления: порог совпадения, масштаб, количество сканируемых кадров.
2. На каждом кадре фиксируется положение логотипа. Приложение собирает соседние кадры, в которых знак сместился, и вычисляет медианный «чистый» фрагмент сцены.
3. Фрагмент подставляется через `cv2.seamlessClone`, а при отсутствии подходящих доноров используется аккуратный `inpaint`.

В блоке настроек можно указать:

- отдельные папки RAW и директорию для очищенных роликов;
- маску шаблона (порог альфа-канала), параметры масштаба, количество кадров для анализа и «полный скан» каждого кадра;
- поисковый радиус по временной оси, запас по краям зоны, максимально допустимое перекрытие (IoU) и размер пула кадров-доноров;
- режим смешивания (обычный или `mixed`) и fallback-метод inpaint.

За поиск отвечает модуль `workers/watermark_cleaner/watermark_detector.py`. Он автоматически масштабирует шаблон под каждый ролик, сканирует нужное количество кадров (по умолчанию весь ролик) и подбирает координаты без необходимости вручную указывать положение логотипа.

Кнопка **«Замена водяного знака»** запускает воркер `workers/watermark_cleaner/restore.py`. Он принимает все параметры через переменные окружения `WMR_*`, поэтому новые значения автоматически подхватываются. Каждая сессия Chrome может запускать очистку отдельно: в карточке рабочего пространства и в отдельном окне добавлена кнопка для быстрого старта, а результат записывается в собственную папку (глобальную или указанную в настройках сессии).

## Редакторы контента

- **Промпты Sora.** Файлы `prompts_<profile>.txt` сохраняются по профилям, история отправок ведётся отдельно.
- **Промпты картинок.** Управление `image_prompts.txt` с поддержкой JSON, сидов и подсказок стиля.
- **Названия.** Таблица `titles.txt` для переименования скачанных роликов и экспорта итоговых списков.

## Командная панель и быстрые действия

Любые команды можно сохранить в разделе «Настройки → Интерфейс». Они появляются:
- в правой панели быстрых действий;
- в командной палитре (поиск по названию/описанию);
- в верхней панели в виде кнопки «🧭 Команды».

## Telegram

Отдельный раздел позволяет:
- настроить токен, `chat_id`, шаблоны сообщений и расписания;
- быстро отправить тест или заготовленный шаблон в выбранный чат;
- просмотреть историю последних уведомлений и повторно отправить их при необходимости.

## Настройки

Группы настроек делятся на тематические вкладки:
1. **Каталоги.** Пути к RAW/BLURRED/MERGED, история и изображения.
2. **Генерация картинок.** Модели, ограничения, стили и директория вывода.
3. **Автоген.** Паузы между шагами, обработка очередей, параметры журналов.
4. **FFmpeg.** Профили блюра, параметры кодека, предпросмотр.
5. **Chrome.** Пути к бинарям, профили, порты CDP.
6. **YouTube / TikTok.** Очереди автопостинга, расписания и лимиты.
7. **Telegram.** Учётные данные, шаблоны и тестовые сообщения.
8. **Интерфейс.** Плотность журналов, отображение правой панели и пользовательские команды.
9. **Обслуживание и документация.** Очистка кешей, автообновления, справка и полный журнал событий.

Настройки автоматически сохраняются, но при необходимости можно применить их вручную.

## Журналы и мониторинг

Раздел **📜 Журнал процессов** отображает события с временными метками и понятными эмодзи-состояниями (ℹ️, 🔄, ✅, ⚠️, ❌). Карточка «Сейчас» показывает текущий шаг и таймер, а история ограничена 200 записями для поддержания скорости интерфейса. При очистке журналов приложение предлагает подтверждение и обновляет показатели в панели обзора.

Страница **🗒️ Логи сессий** собирает свежие строки из `submitted.log`, `failed.log` и `download.log` каждой сессии. Можно быстро переключаться между рабочими пространствами и проверять, какие ролики отправились, что упало в ошибку и как отработал автоcкач.

## Полезные советы

- Используйте несколько рабочих пространств для одновременной работы с разными профилями Chrome и наборами промптов.
- Сохраняйте шаблоны Telegram, чтобы повторно отправлять стандартные уведомления в один клик.
- Проверяйте квоты генерации изображений — уведомления предупредят о приближении суточного лимита.
- Командная палитра помогает запускать популярные сценарии без переключения вкладок.
